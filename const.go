package main

const (
	// For resolution
	windowWidth  = 800
	windowHeight = 600
	screenWidth  = 320
	screenHeight = 240

	// For game objects
	// multiply scaleW and imageW for measurements
	playerScaleWidth  = 4
	playerScaleHeight = 16
	imageWidth        = 3
	imageHeight       = 3
	playerWidth       = playerScaleWidth * imageWidth
	playerHeight      = playerScaleHeight * imageHeight
)
